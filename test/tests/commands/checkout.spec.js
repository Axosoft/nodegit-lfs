import {
  todo
} from '../../utils';

describe('checkout', () => {
  describe('the default export', () => {
    it('runs checkout in the provided repo', todo);

    it('handles errors', todo);
  });
});
