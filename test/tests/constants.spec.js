import {
  todo
} from '../utils';

describe('Constants', () => {
  describe('Regexes', () => {
    it('GIT', todo);

    it('LFS', todo);

    it('PASSWORD', todo);

    it('SKIPPED_BYTES', todo);

    it('SKIPPED_FILES', todo);

    it('TOTAL_BYTES', todo);

    it('TOTAL_FILES', todo);

    it('TRACK', todo);

    it('USERNAME', todo);

    it('VERSION', todo);
  });
});
